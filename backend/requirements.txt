# Python 3.11 compatible - all versions pinned for stability

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# LangGraph + LangChain (compatible set)
# langgraph>=0.2.0: interrupt 和 Command 正式移入 langgraph.types
# langchain-core>=0.3: AIMessage/HumanMessage/SystemMessage 路径稳定
langgraph==0.2.55
langchain-core==0.3.21
langchain-openai==0.2.12
langchain-community==0.3.9

# HTTP client
httpx==0.27.0

# Redis (async)
redis==5.0.6

# PostgreSQL (async)
asyncpg==0.29.0

# Auth / JWT
python-jose[cryptography]==3.3.0

# Settings / env
pydantic==2.10.3
pydantic-settings==2.7.1

# Utilities
python-dotenv==1.0.1
